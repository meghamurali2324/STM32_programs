/**
 ******************************************************************************
 * @file           : main.c
 * @author         : megha
 * @brief          : Blinking an LED on PA5 (Nucleo Board LED)
 ******************************************************************************
 */

#include <string.h>
#include "stm32f446xx.h"
#include "stm32_GPIO_Driver.h"

/**
 * @brief Software delay function
 * @note  Used 'volatile' to prevent compiler optimization from removing the loop
 */
void delay(void) {
    for(volatile uint32_t i = 0; i < 500000; i++);
}

int main(void)
{
    GPIO_Handle_t GpioLed;



    // 2. Configure the GPIO Pin Settings
    GpioLed.pGPIOx = GPIOA;
    GpioLed.GPIO_PinConfig.GPIO_PinNumber      = GPIO_PIN_NO_5;
    GpioLed.GPIO_PinConfig.GPIO_PinMode        = GPIO_MODE_OUT;
    GpioLed.GPIO_PinConfig.GPIO_PinSpeed       = GPIO_SPEED_FAST;
    GpioLed.GPIO_PinConfig.GPIO_PinOPType      = GPIO_OP_TYPE_PP;
    GpioLed.GPIO_PinConfig.GPIO_PinPuPdControl = GPIO_NO_PUPD;

    // 3. Initialize the peripheral
    GPIO_Init(&GpioLed);

    while(1)
    {
        // 4. Toggle the LED
        GPIO_ToggleOutputPin(GPIOA, GPIO_PIN_NO_5);

        // 5. Wait
        delay();
    }

    return 0;
}
